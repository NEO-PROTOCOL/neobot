
graph TB
    subgraph NEOBOT["🎯 NEOBOT CORE (Orchestrator)"]
        CORE["Core Gateway"]
        MIO["MIO System (Identity Backend)"]
        ADAPTER["🛡️ Sovereign Adapter"]
        
        %% Internal Flow
        CORE --> ADAPTER
        ADAPTER --> MIO
    end

    subgraph OPERATIONS["💼 OPERATIONS & SALES"]
        FLOWCLOSER["FlowCloser (WhatsApp/Telegram CRM)"]
        AGENT_FO["Agent FlowOFF (Traffic/Sales)"]
        Landing["FlowOFF.xyz Landing"]
        
        %% Sales Funnel
        Landing --> AGENT_FO
        AGENT_FO <--> FLOWCLOSER
        FLOWCLOSER <--> CORE
    end

    subgraph ENGINEERING["🏭 NEO SMART FACTORY (Web3 Engineering)"]
        FACTORY["Smart Factory Hub"]
        S_CORE["Smart Core Contracts"]
        S_UI["Smart UI (Deployer)"]
        
        %% Factory Flow
        S_UI --> S_CORE
        S_CORE -- Deploys --> FACTORY
    end

    subgraph FINANCIAL["💰 FLOWPAY (Sovereign Finance)"]
        FP_GATE["FlowPay Gateway"]
        FP_CORE["FlowPay Core"]
        LIQUIDITY["Liquidity Pools"]
        
        %% Money Flow
        FP_GATE --> FP_CORE
        FP_CORE -- Settles on --> LIQUIDITY
    end

    %% Key Integrations (The "Connections")
    CORE -.->|Auth/Identity| MIO
    CORE <-->|Orchestrates| OPERATIONS
    CORE <-->|Verifies| FINANCIAL
    
    %% Cross-Domain Connections
    ENGINEERING -- Deploys Token for --> FINANCIAL
    OPERATIONS -- Generates Revenue for --> FINANCIAL
    
    %% Infrastructure Layer (Simplified)
    subgraph INFRA["☁️ SOVEREIGN INFRA"]
        RAILWAY["Railway (Hosting)"]
        BASE["Base Chain (L2 Settlement)"]
    end
    
    NEOBOT -.-> RAILWAY
    FINANCIAL -.-> BASE
    ENGINEERING -.-> BASE

    %% Styling
    classDef core fill:#ff4444,stroke:#aa0000,stroke-width:3px,color:#fff
    classDef ops fill:#ff00ff,stroke:#aa00aa,stroke-width:2px,color:#fff
    classDef eng fill:#00ccff,stroke:#0088aa,stroke-width:2px,color:#000
    classDef fin fill:#00ffcc,stroke:#00aba9,stroke-width:2px,color:#000
    classDef infra fill:#333,stroke:#666,stroke-width:1px,color:#fff
    
    class CORE,MIO,ADAPTER core
    class FLOWCLOSER,AGENT_FO,Landing ops
    class FACTORY,S_CORE,S_UI eng
    class FP_GATE,FP_CORE,LIQUIDITY fin
    class RAILWAY,BASE infra
